<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bink.igottamealing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MealingApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_restaurant_orange_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_restaurant_orange_24dp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.CategoryActivity"
            android:label="@string/title_activity_category"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".view.activity.MealDetailsActivity"
            android:label="@string/title_activity_meal_details"
            android:theme="@style/AppTheme" />
    </application>

</manifest>